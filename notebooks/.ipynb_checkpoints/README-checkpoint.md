# Notebooks

Drop ad-hoc DuckDB or Python notebooks in this folder if you want to explore the Parquet outputs locally (outside of Dremio). It is ignored by the main pipeline and kept empty by default.
